# Programación orientada a objetos

Ejemplos para las clases 22–25.

- `instrumentos.py`: clase, invariante y composición.
- `sensores.py`: polimorfismo mediante una interfaz común.

Se compara explícitamente con una solución basada en funciones. La herencia solo
se usa cuando existe una relación de sustitución justificable.

