# Control de flujo

Ejemplos para las clases 10 y 11.

- `clasificar_medicion.py`: decisiones exclusivas y validación previa.
- `acumular_trabajo.py`: patrón de acumulación en un ciclo.

El propósito es razonar sobre casos y estados, no memorizar plantillas de `if` o
`for`.

