# Archivos, rutas y excepciones

Ejemplos para la clase 13.

- `leer_caida.py`: lectura CSV con `pathlib`, contexto y errores específicos.
- `mediciones_pequenas.csv`: entrada autónoma del ejemplo.

El ejemplo distingue error de formato, archivo ausente y dato físicamente
inválido. No se usa una excepción general para ocultar fallas.

