# Configuración de WSL2 para el curso

## Integrantes y equipo usado

- Integrantes:
- Versión de Windows:
- Distribución Linux:

## 1. Instalación o verificación

Explique qué hace `wsl --install -d Ubuntu`. Si WSL ya estaba instalado, indique
qué comprobó en lugar de reinstalarlo.

Pegue una salida breve y sin datos sensibles de:

```powershell
wsl --status
wsl --list --verbose
```

Explique cómo sabe que la distribución usa WSL2.

## 2. Primer inicio y Git

Describa la creación del usuario Linux y los comandos separados usados para
actualizar el índice de paquetes, instalar Git y verificar su versión.

## 3. Ubicación del proyecto

Indique la ruta Linux elegida para el proyecto y explique la diferencia entre
trabajar en `~/...` y acceder a archivos de Windows mediante `/mnt/c/...`.

## 4. Ejecución de un `.sh`

Explique el *shebang*, `bash archivo.sh`, `chmod u+x archivo.sh` y
`./archivo.sh`. Incluya una comprobación breve realizada por el equipo.

## 5. Git y GitHub

Explique con sus palabras la diferencia entre directorio de trabajo, área de
preparación, commit local y repositorio remoto. No incluya tokens ni contraseñas.

## 6. Problema encontrado y solución

Documente un error real o una comprobación preventiva: mensaje, causa y solución
verificada. No sugiera borrar o desregistrar una distribución.

## 7. Fuentes consultadas

Liste la documentación consultada y la fecha de acceso.

